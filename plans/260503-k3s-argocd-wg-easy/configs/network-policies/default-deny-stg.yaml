# Chặn toàn bộ ingress traffic vào namespace stg mặc định
# Chỉ cho phép traffic từ Traefik (qua VPN middleware) và traffic nội bộ
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny-ingress
  namespace: stg
spec:
  podSelector: {}
  policyTypes:
    - Ingress
