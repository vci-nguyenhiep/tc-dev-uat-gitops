apiVersion: secrets.hashicorp.com/v1beta1
kind: VaultAuth
metadata:
  name: stg-auth
  namespace: stg
spec:
  method: kubernetes
  mount: kubernetes
  kubernetes:
    role: stg-role
    serviceAccount: default
    audiences:
      - vault
