apiVersion: secrets.hashicorp.com/v1beta1
kind: VaultAuth
metadata:
  name: uat-auth
  namespace: uat
spec:
  method: kubernetes
  mount: kubernetes
  kubernetes:
    role: uat-role
    serviceAccount: default
    audiences:
      - vault
