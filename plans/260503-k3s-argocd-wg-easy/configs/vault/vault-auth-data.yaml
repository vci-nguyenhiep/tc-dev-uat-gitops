apiVersion: secrets.hashicorp.com/v1beta1
kind: VaultAuth
metadata:
  name: data-auth
  namespace: data
spec:
  method: kubernetes
  mount: kubernetes
  kubernetes:
    role: data-role
    serviceAccount: default
    audiences:
      - vault
