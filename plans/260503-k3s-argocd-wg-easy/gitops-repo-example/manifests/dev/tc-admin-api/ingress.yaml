apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: tc-admin-api
  namespace: dev
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
    traefik.ingress.kubernetes.io/router.middlewares: dev-vpn-only@kubernetescrd
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - dev-tc-admin-api.company.com
      secretName: dev-tc-admin-api-tls
  rules:
    - host: dev-tc-admin-api.company.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: tc-admin-api
                port:
                  number: 80
