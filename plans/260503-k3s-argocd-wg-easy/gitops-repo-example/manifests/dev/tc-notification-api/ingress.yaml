apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: tc-notification-api
  namespace: dev
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
    traefik.ingress.kubernetes.io/router.middlewares: dev-vpn-only@kubernetescrd
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - dev-tc-notification-api.company.com
      secretName: dev-tc-notification-api-tls
  rules:
    - host: dev-tc-notification-api.company.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: tc-notification-api
                port:
                  number: 80
