apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: tc-admin-api
  namespace: uat
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
    traefik.ingress.kubernetes.io/router.middlewares: uat-vpn-only@kubernetescrd
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - uat-tc-admin-api.company.com
      secretName: uat-tc-admin-api-tls
  rules:
    - host: uat-tc-admin-api.company.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: tc-admin-api
                port:
                  number: 80
